正确待签名字符串

v2|6b2adbe76660803d1ef3da107e3bc2ca8bb1dfee73642598c37b31012139c7c6|gateway-client|ui|operator|operator.admin,operator.read,operator.write|1783100859347|993f75cc-4dc5-4fa2-953a-9d030988ab7c|

注意：



最后有一个 |，因为token字段是空的，必须保留这个分隔符

完全按照上面这个字符串计算签名，肯定能通过验证

📝 完整签名计算代码示例（JavaScript）

复制

// 你的参数

const deviceId = '6b2adbe76660803d1ef3da107e3bc2ca8bb1dfee73642598c37b31012139c7c6';

const clientId = 'gateway-client';

const clientMode = 'ui';

const role = 'operator';

const scopes = \['operator.admin', 'operator.read', 'operator.write'];

const signedAt = 1783100859347;

const nonce = '993f75cc-4dc5-4fa2-953a-9d030988ab7c';

const token = ''; // password认证为空



// ✅ 正确拼接待签名字符串

const message = \[

&#x20; 'v2',

&#x20; deviceId,

&#x20; clientId,

&#x20; clientMode,

&#x20; role,

&#x20; scopes.join(','), // 数组转逗号分隔字符串

&#x20; signedAt.toString(), // 数字转字符串

&#x20; nonce,

&#x20; token

].join('|');



// ✅ 然后用你的ed25519私钥签名message.getBytes()

// ✅ 结果base64编码，放到device.signature字段



console.log(message);

// 输出就是我上面给你的那个字符串

